# Core dependencies
fastapi==0.109.1
uvicorn[standard]==0.27.0
pydantic==2.9.2
pydantic-settings==2.6.1
python-multipart==0.0.18

# Computer Vision
# IMPORTANT: Version pins are required for numpy 2.x compatibility
# - mediapipe 0.10.14 is the last version with the legacy solutions API (mp.solutions.face_detection)
# - It installs opencv-contrib-python 4.13.0.90 which is numpy 2.x compatible
# - Later versions (0.10.18+) removed the solutions API and require code changes
# - DO NOT remove version constraints or update to 0.10.18+ without updating the code
mediapipe==0.10.14
Pillow==10.4.0

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
httpx==0.26.0

# Development
black==24.1.1
flake8==7.0.0
mypy==1.8.0
